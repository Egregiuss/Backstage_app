name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Write SSH key to file
        shell: bash
        env:
          VM_SSH_KEY: ${{ secrets.VM_SSH_KEY }}
        run: |
          mkdir -p "$RUNNER_TEMP/ssh"
          printf '%s\n' "$VM_SSH_KEY" > "$RUNNER_TEMP/ssh/vm_deploy_key"
          chmod 600 "$RUNNER_TEMP/ssh/vm_deploy_key"

      - name: Deploy to VM
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key_path: ${{ runner.temp }}/ssh/vm_deploy_key
          port: 22
          script_stop: false
          script: |
            set -e

            echo "Starting deployment..."

            cd ~/Backstage_app

            echo "Pulling latest code..."
            git fetch origin main
            git reset --hard origin/main

            echo "Installing dependencies..."
            yarn install --immutable

            echo "Stopping existing PM2 processes (if any)..."

            if pm2 list | grep -q backstage; then
              pm2 delete backstage
            fi

            if pm2 list | grep -q backstage-frontend; then
              pm2 delete backstage-frontend
            fi

            if pm2 list | grep -q backstage-backend; then
              pm2 delete backstage-backend
            fi

            pkill -f "yarn dev" || true
            pkill -f "yarn start" || true

            echo "Starting Backstage..."

            HOST=0.0.0.0 \
            APP_BASE_URL='${{ secrets.APP_BASE_URL }}' \
            BACKEND_BASE_URL='${{ secrets.BACKEND_BASE_URL }}' \
            pm2 start "yarn dev" --name backstage --update-env

            pm2 save

            echo "Deployment complete!"

