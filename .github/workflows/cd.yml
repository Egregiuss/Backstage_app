name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Prepare SSH key
      - name: Write SSH key to file
        shell: bash
        env:
          VM_SSH_KEY: ${{ secrets.VM_SSH_KEY }}
        run: |
          mkdir -p "$RUNNER_TEMP/ssh"
          printf '%s\n' "$VM_SSH_KEY" > "$RUNNER_TEMP/ssh/vm_deploy_key"
          chmod 600 "$RUNNER_TEMP/ssh/vm_deploy_key"

      # Step 2: Deploy via SSH
      - name: Deploy Backstage to GCP VM
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key_path: ${{ runner.temp }}/ssh/vm_deploy_key
          script: |
            set -e
            cd ~/Backstage_app

            # Pull latest code
            git fetch origin main
            git reset --hard origin/main

            # Delete previous PM2 process if exists
            pm2 delete backstage || true

            # Start Backstage with proper environment variables
            APP_BASE_URL=${{ secrets.APP_BASE_URL }} BACKEND_BASE_URL=${{ secrets.BACKEND_BASE_URL }} pm2 start "yarn start" --name backstage

            # Save PM2 process list
            pm2 save